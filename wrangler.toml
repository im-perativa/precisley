# Optional. Cloudflare Pages can also be configured only in the dashboard.
# Dashboard: framework Vite, build `npm run build`, output `dist`, Node 18.
name = "precisley"
compatibility_date = "2026-09-21"
pages_build_output_dir = "dist"
