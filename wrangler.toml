# Workers Builds (this project's dashboard): no "output directory" field.
# dist/ is declared here. No Worker script — static Vite SPA only.
# Dashboard: build `npm run build`, deploy `npx wrangler deploy`, Node 20.
name = "precisley"
compatibility_date = "2026-09-21"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
